subprojects {
	apply plugin: 'groovy'
	apply plugin: 'idea'

	group = 'at.dinauer.fcw'
	
	repositories {
		mavenCentral()
	}
	
	dependencies {
		groovy group: 'org.codehaus.groovy', name: 'groovy', version: '2.0.5'
		testCompile group: 'junit', name: 'junit', version: '4.+'
	}
}